"""Integration primitives (protocols + control loop helpers).

This package intentionally avoids infrastructure dependencies.

The goal is to let teams plug adaptive_experimentation into their existing
allocation/config systems and observation pipelines with minimal glue.
"""
